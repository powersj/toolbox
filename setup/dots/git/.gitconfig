[user]
    name = Joshua Powers
    email = josh.powers@canonical.com
    signingkey = DA660C25

[push]
    default = simple

[log]
    decorate = auto

[alias]
    l = log --color --graph --pretty=format:'%Cgreen%ad %Cred%h%Creset -%C(yellow)%d%Creset %s %C(bold blue)[%an]%Creset' --date=short --abbrev-commit

[core]
    editor = vim

[color]
    ui = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red
    new = green
[color "status"]
    added = yellow
    changed = green
    untracked = cyan

[gitubuntu]
    lpuser = powersj

[man "git-ubuntu-man"]
    cmd = git-ubuntu.man
[man]
    viewer = git-ubuntu-man

[url "git+ssh://powersj@git.launchpad.net/"]
    insteadof = lp:
[url "git+ssh://powersj@git.launchpad.net/~ubuntu-server-dev/ubuntu/+source/"]
    insteadOf = ldusdp:
[url "git+ssh://powersj@git.launchpad.net/~usd-import-team/ubuntu/+source/"]
    insteadOf = lpusip:
[url "git+ssh://powersj@git.launchpad.net/~powersj/ubuntu/+source/"]
    insteadOf = lpmep:

